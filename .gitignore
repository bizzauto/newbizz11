# Dependencies (huge folder, installs via npm ci)
node_modules/
/.pnp
.pnp.js

# Environment files (SECRETS - NEVER upload!)
.env
.env.local
.env.*.local
.env.production

# Build output (generated by npm run build, not needed in repo)
dist/
build/
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Uploads (user files, not part of code)
uploads/

# IDE settings (personal, not needed)
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# OS junk files
.DS_Store
Thumbs.db
desktop.ini

# Testing
coverage/
.nyc_output/

# Prisma migrations - MUST be committed for deployment!
# prisma/migrations/  <-- REMOVED: migrations must be in repo for prisma migrate deploy

# Docker backups
backups/

# Misc temp files
.cache/
.temp/
tmp/

# Kilo agent worktrees (stale copies, not part of project)
.kilo/

# Deployment agent (local tool, not part of project)
deploy-agent/

# Extra files / mobile app
EXTRA/
# Track mobile-app source (Capacitor config, native manifests) but ignore
# heavy build artifacts and dependencies.
mobile-app/node_modules/
mobile-app/android/app/build/
mobile-app/android/.gradle/
mobile-app/android/local.properties
mobile-app/ios/App/Pods/
mobile-app/ios/App/build/
mobile-app/ios/App/DerivedData/
*.zip

# Platform-specific generated files (auto-created by deploy agent)
render.yaml
vercel.json
fly.toml

# AI/Editor config (personal settings)
.continue/
.qodo/
.qwen/
.roo/
.claude/autoharness/

# Windows reserved device names
/nul
nul

# Accidental nested repos
MY\ PROJECT*/

# Android local SDK path (machine-specific, not in repo)
/local.properties
local.properties

.envorg
.envorg.txt

# Secrets & private keys
.encryption.key
*.pem
bizzauto-new-key
bizzauto-new-key.pub
temp_vps_key*
coolify_cookies.txt
coolify_response.html

# Build artifacts & logs
build-output*.txt
output.txt
output.log
server.log
server.err
data.sql

# One-off fix scripts (root)
_fix_login.*
fix-whatsapp.*
setup-test-user.*
fix-query-types.ts

# Misc
d.pdf
screenshot*.png
screenshot*.jpg
npx
session-memory.md

# Audit reports and build logs
*.txt
DEPLOYMENT_GUIDE.md
audit-report-*.md
.DEPLOYMENT_GUIDE.md
.audit-report-*.md
DESIGN.md


# Env files with secrets (never commit)
.env11
.env22
.env (2).example
 (2).env11
env.txt
extraenv.txt
*.env11
*.env22
*.env22

# deployment helper with secrets — NEVER commit
coolify-env-ready.txt
